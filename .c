





-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#include <stdio.h>
int main() {
  int N;
  scanf("%d", & N);
  int D1 = 2, D2 = 1;
  for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
      if (i == j) {
        printf("0 ");
      } else if (j > i && i < N / 2) {
        printf("%d ", D1);
        D1 += 2;
      } else {
        printf("%d ", D2);
        D2 += 2
      }
    }
    printf("\n");
  }
}
