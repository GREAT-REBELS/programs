The program must accept N integers and an integer X as the input. The program must print the count of consecutive X integers (among N integers) in which all the X integers 
are different as the output.
Example1:
Input:
6 3
1 2 3 4 4 5
Output:
2
Example2:
Input:
8 5
9 8 1 8 1 2 4 5
Output:
1
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#include <stdio.h>
int main()
{
int N,X,count=0;
scanf("%d %d",&N,&X);
int arr[N];
for(int i=0;i<N;i++)
{
    scanf("%d",&arr[i]);
}
for(int i=0;i<=X;i++)
{
    if(X+i<=N)
    {
   int  arrD[100001]={0},flg=0;
    for(int j=i;j<X+i;j++)
    {
        arrD[arr[j]]++;
    if(arrD[arr[j]]==1)
    {
            flg=1;
    }
        else
    {
            flg=0;
            break;
    }
    }
    if(flg==1)
    {
        count++;
    }
    }
}
printf("%d",count);
}
